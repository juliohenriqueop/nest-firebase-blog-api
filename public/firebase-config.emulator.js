/* eslint-disable @typescript-eslint/no-unused-vars */

const firebaseConfig = {
  apiKey: 'EMULATOR-WORKS-WITH-ANY-API-KEY',
};

// Will not use emulator when not present
const firebaseAuthEmulatorConfig = {
  url: 'http://localhost:9099',
};
